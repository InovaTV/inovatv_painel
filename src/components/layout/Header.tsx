export default function Header() {
  return (
    <header
      style={{
        height: 64,
        background: "#0f172a",
        color: "#fff",
        display: "flex",
        alignItems: "center",
        padding: "0 24px",
        fontSize: 22,
        fontWeight: 700,
      }}
    >
      InovaTV Painel
    </header>
  );
}